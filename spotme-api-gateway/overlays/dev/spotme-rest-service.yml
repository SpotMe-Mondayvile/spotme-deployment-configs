apiVersion: v1
kind: Service
metadata:
    name: ${appname}
    annotations:
      strategy.spinnaker.io/recreate: 'true'
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30280
  selector:
    app: ${appname}