apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ${namespace}

commonLabels:
  # serviceGroup: spotme-rest
  project: spotme

resources:
  - ../../base
  - configmap.yml
  
configMapGenerator:
  - name: ${parameters['appname']}-properties
    files:
      - application-k8s.yaml

images:
  - name: app-image
    newName: ${image}

patches:
- replicaset-patch.yml 
- service-patch.yml