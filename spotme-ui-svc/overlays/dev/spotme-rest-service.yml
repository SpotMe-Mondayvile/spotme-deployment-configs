apiVersion: v1
kind: Service
metadata:
    name: spotme-backend-service
    annotations:
      strategy.spinnaker.io/recreate: 'true'
spec:
  type: NodePort
  selector: 
    app: ${appname}
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30280

