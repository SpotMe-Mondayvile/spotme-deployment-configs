apiVersion: v1
kind: Service
metadata:
  name: ${appname}
  labels:
    app: ${appname}
spec:
  type: NodePort
  selector:
    app: ${appname}
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30206
