apiVersion: v1
kind: Service
metadata:
    name: ${appname}
    annotations:
      strategy.spinnaker.io/recreate: 'true'
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30187
  selector:
    app: ${appname}