apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${appname}
  labels:
    app: ${appname}
    type: back-end
spec:
  template:
    spec:
      containers:
        - name: ${appname}
          image: app-image
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
    metadata:
      name: ${appname}
      labels:
        app: ${appname}
        type: back-end
        project: spotme
  replicas: 1
  selector:
    matchLabels:
      app: ${appname}
